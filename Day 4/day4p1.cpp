#include <iostream>
#include <fstream>
#include <string>

using namespace std;

const string inputFileName = "input.txt";

int main() {

    ifstream inputFile(inputFileName);

    string line;
    while(getline(inputFile, line)) {

    }

    return 0;
}
